# Regenerated by steam3.py / drip.py — ~24 MB of frames, ~2 minutes to rebuild
steam/
drip/
# Renders. These are large and belong outside the repo.
hq*.mp4
*.mp4
# HuggingFace model cache (~3.7 GB) — only depth2.py and sam_masks.py need it,
# and sam/ is committed so neither has to run.
hf/
# Third-party footage, and the screen program baked from it by screen.py.
src/
screen/
# Generated from graph.py on every render.
graph5.gen.txt
# Scratch
*.prev.png
__pycache__/
